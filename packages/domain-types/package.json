{
  "name": "@platform/domain-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TS types/enums mirrored from prisma/schema.prisma — the single source every app imports domain shapes from.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0",
    "@platform/config": "workspace:*"
  }
}
