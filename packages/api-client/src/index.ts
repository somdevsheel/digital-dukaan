// Populated Phase 7 (once @app/api emits its OpenAPI spec) — `pnpm generate` regenerates
// src/generated/schema.d.ts from the live spec; this file wraps it in a typed openapi-fetch client.
// Never hand-author request/response types here — see Architecture §8 on spec-from-code.
export {};
